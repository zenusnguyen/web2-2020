{
  "routes": [
    {
      "method": "post",
      "path": "/getotp",
      "handler": "email.sendMail",
      "config": {
        "policies": []
      }
    },
    {
      "method": "post",
      "path": "/getotp-verify",
      "handler": "email.sendMailVerify",
      "config": {
        "policies": []
      }
    }
  ]
}
